meta {
  name: Change Password
  type: http
  seq: 2
}

post {
  url: {{host}}/api/profile/change_password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "currentPassword": "P@ssw0rd",
    "newPassword": "12345678"
  }
}
