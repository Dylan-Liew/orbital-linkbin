meta {
  name: Change Usernamex
  type: http
  seq: 1
}

post {
  url: {{host}}/api/profile/change_username
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "newUsername": "any new name"
  }
}
